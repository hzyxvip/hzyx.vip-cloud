---
description: 公司资料设定 UI 未经用户同意不得改动
globs: src/views/data/DataCompany*.vue
alwaysApply: false
---

# 公司资料设定 UI 保护

用户对「公司资料设定」模块的 UI 设计非常满意，视为已定稿标准。

## 约束

- **未经用户明确同意**，不得修改以下页面的布局、样式、交互或视觉结构：
  - `DataCompany.vue`（公司资料设定主页）
  - `DataCompanyIntro.vue`
  - `DataCompanyInvestment.vue`
  - `DataCompanyLicense.vue`
- 其他页面如需对齐公司资料设定风格，应**复制/参考**其设计，而不是反向改动公司资料设定页。
- 若任务涉及公司资料设定 UI，先说明拟改内容并等待用户确认后再动手。

## 已定稿：企业简介与浅绿边框（`DataCompany.vue`）

用户已确认定稿，**不得再改**以下交互与样式，除非用户再次明确要求：

### 「本企业资料」「企业介绍」浅绿边框
- 两块卡片使用 `intro-content-zone` 类，**常驻**浅绿色边框：
  - `border: 1px solid #b2f0e3`
  - `box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06), inset 0 0 0 1px rgba(178, 240, 227, 0.35)`
- **不要**改为点击才显示、定时消失、或更强的高亮边框。

### 「企业简介」快捷入口
- **不跳转**子页面（无 `/data/company/intro`）。
- 点击后：平滑滚动至「本企业资料」+ `ElMessage.info` 提示内容已在下方两块区域展示。
- 「招商信息」「企业证照」仍跳转各自子页面。


- 纯逻辑、数据、校验、接口修复，且不改变可见 UI 时，可在必要时修改脚本部分；若会连带影响样式或布局，仍须先征得用户同意。
